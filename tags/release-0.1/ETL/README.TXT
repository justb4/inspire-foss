This directory contains the Extract Transfer Load implementations specific to each
EU country/NMA within subdirectories. The ETL process should deliver SQL to be inserted
into the PostGIS DB defined under the directory "db".
